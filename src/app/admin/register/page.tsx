'use client';
import React,{useState} from 'react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
import {Logo} from '@/components/ui/Logo';
import {api} from '@/services/api';
import {useToast} from '@/context/ToastContext';
export default function AdminRegisterPage(){const router=useRouter();const{showToast}=useToast();const[form,setForm]=useState({name:'',email:'',password:'',setupKey:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');setLoading(true);try{await api.registerInitialAdmin(form);showToast('Initial Super Admin account created.','success');router.push('/admin/login');}catch(e:any){setError(e?.message??'Could not create the initial admin account.');}finally{setLoading(false);}};return <main style={{minHeight:'100vh',background:'#07090E',display:'grid',placeItems:'center',padding:24}}><form onSubmit={submit} style={{width:'100%',maxWidth:440,background:'#0F131C',border:'1px solid rgba(255,255,255,.1)',borderRadius:12,padding:32,display:'grid',gap:16}}><div style={{textAlign:'center'}}><Logo variant="admin" priority/><h1 style={{color:'#FFF',fontSize:'1.35rem',marginTop:18}}>Create initial Super Admin</h1><p style={{color:'#9CA3AF',fontSize:'.85rem'}}>For local development, enter any setup key. Production requires the configured backend key.</p></div>{error&&<div style={{padding:12,borderRadius:6,background:'rgba(239,68,68,.1)',color:'#FCA5A5'}}>{error}</div>}{[['name','Full name','text'],['email','Email address','email'],['password','Password (12+ characters)','password'],['setupKey','Setup key (any text locally)','password']].map(([key,placeholder,type])=><input key={key} required minLength={key==='password'?12:undefined} type={type} placeholder={placeholder} value={(form as any)[key]} onChange={e=>setForm({...form,[key]:e.target.value})} style={{padding:13,background:'#07090E',border:'1px solid rgba(255,255,255,.14)',borderRadius:6,color:'#FFF'}}/>)}<button disabled={loading} style={{padding:13,background:'#C9A96E',borderRadius:6,color:'#07090E',fontWeight:700,cursor:'pointer'}}>{loading?'Creating…':'Create Super Admin'}</button><Link href="/admin/login" style={{textAlign:'center',color:'#C9A96E',fontSize:'.85rem'}}>Back to admin login</Link></form></main>}
